---
- hosts: localhost

  vars:
    enable_registration: false

  tasks:
    - name: Install apt-utils
      apt:
        name:
          - apt-utils
        install_recommends: no
        state: present
      when: ansible_os_family == 'Debian'

  roles:
    - role_under_test
